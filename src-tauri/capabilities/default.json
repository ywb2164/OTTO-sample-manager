{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Capabilities required by the OTTO main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "opener:allow-default-urls",
    "opener:allow-reveal-item-in-dir",
    "updater:default"
  ]
}
